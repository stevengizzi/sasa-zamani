# Sprint 2, Session S1: Backend xs Computation + API Response Enrichment

**Date:** 2026-03-19

## Changes

- Added `compute_xs()` to `app/clustering.py` — maps cluster names to canonical
  x-positions on the inward-social spectrum, applies per-event offset and
  deterministic jitter
- Extended `insert_event()` with optional `xs` parameter
- Added `update_event_xs()` for backfill use
- Enriched `EventResponse` with `xs`, `day` fields
- Enriched `ClusterResponse` with `glyph_id`, `myth_text`, `is_seed` fields
- Created `scripts/backfill_xs.py` (idempotent)
- 10 new tests (97 pass, 3 skip non-integration)

## Notes

- `compute_xs` not yet wired into live Telegram/Granola pipeline (those files
  are frozen this session). Backfill script covers existing data.
- Pre-existing integration test failures (stale DB with 12 clusters) unchanged.
